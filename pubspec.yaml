name: app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '^3.9.0'

dependencies:
  flutter:
    sdk: flutter
  # ------------------------
  # Google Maps
  # ------------------------
  google_maps_flutter: ^2.13.1

  # ------------------------
  # State Management
  # ------------------------
  bloc: ^8.1.2
  flutter_bloc: ^8.1.3
  hydrated_bloc: ^9.1.0

  # ------------------------
  # UI / Responsiveness
  # ------------------------
  flutter_screenutil: ^5.9.0
  font_awesome_flutter: ^10.7.0

  # ------------------------
  # Networking
  # ------------------------
  dio: ^5.7.0
  http: ^1.2.0
  
  # ------------------------
  # Dependency Injection
  # ------------------------
  get_it: ^7.6.7

  # ------------------------
  # Local Storage
  # ------------------------
  path_provider: ^2.1.2
  shared_preferences: ^2.3.2

  # ------------------------
  # Environment & Config
  # ------------------------
  flutter_dotenv: ^5.1.0

  
  # ------------------------
  # Utils
  # ------------------------
  equatable: ^2.0.5
  image_picker: ^1.2.0
  intl: ^0.20.2
  url_launcher: ^6.3.2
  file_picker: ^8.1.2
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/videos/
    - assets/images/
    - assets/images/pin_verde.png
    - assets/images/pin_morado.png
    - assets/images/Citydriver-rafiki.png
    - assets/images/iconografia/
    - assets/images/iconografia/dark/
    - assets/images/iconografia/dark/delivery.png